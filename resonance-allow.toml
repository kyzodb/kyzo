# Story #81: the resonance gate's one allowlist. Every entry below cites an
# issue or states a plain reason; xtask itself rejects an entry with no
# real citation (see crates/xtask/src/allowlist.rs). This file is read from the
# repo root by every one of the five checks, including inside a bite-proof's
# throwaway rsync copy (RESONANCE_ROOT points there; this file travels with
# the copy since it's part of the repo tree being copied).
#
# check 1 (derive-bypass): zero entries needed -- the tree is clean today
# (Interval and RelationId both already hand-write Deserialize; see the
# story report for the bite-proof reintroducing the historical shape).

# ---------------------------------------------------------------------------
# check 2: panic-on-hostile-bytes lint
# ---------------------------------------------------------------------------
# Decode surfaces live in kyzo-model (value plane) and store/time.rs.
# Production decode paths no longer carry length-checked `.expect(...)`
# (copy_from_slice / typed Err). Zero panic_lint waivers needed today.

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/plan/stratify.rs::aggregation_character",
]
citation = "Issue #89's ruling: the three REFERENCE-tier copies of this classification (query/laws.rs, query/provenance.rs, query/trials.rs -- they judge the engine, never each other) are consolidated into one shared query/laws.rs::head_classes, pub(crate), consumed by provenance.rs and trials.rs; likewise unify/ground/Bindings, and trials.rs::check_unify (drift, not a genuine second algorithm -- it never independently judged unify itself) folded into the same shared unify. The pairs that remain, and must remain, unwaived-but-cited: query/stratify.rs::aggregation_character is the ENGINE's copy, deliberately hand-maintained independent of the oracle's head_classes -- the refusal-corpus differential (the_oracle_refusal_corpus_is_refused, stratify.rs) is only meaningful because the engine's stratifiability boundary and the oracle's are two separately-authored implementations that happen to agree; sharing this one would collapse that differential into a tautology. Likewise query/incremental.rs's whole IVM surface (unify, collect_candidates, contribute_candidates_subset, body_bindings_from, eval_aggregating_head_incremental) is an INDEPENDENTLY-WRITTEN twin of laws.rs's incremental_eval by its own module doc: 'never a shared import ... so a bug cannot hide behind one implementation covering for the other', with the transitive proof chain (production == oracle incremental == naive recompute) resting on that independence. See the doc comment at aggregation_character's definition (stratify.rs), incremental.rs's own module doc, and head_classes'/unify's definitions (laws.rs) for the full argument. A mutant that broke the shared unify (issue #89's verification) failed laws/provenance/trials together while stratify's and incremental's own suites stayed fully green, confirming this independence is real, not assumed. (Story #321: the head_classes closure line drifted from @750 to @766 since this entry was written; repointed, not re-worded.)"

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::add_vecs",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::mul_vecs",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_acos",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_acosh",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_add",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_asin",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_asinh",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_atan",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_atanh",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_bit_and",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_bit_xor",
  "crates/kyzo-core/src/exec/stdlib/collection.rs::op_chunks",
  "crates/kyzo-core/src/exec/stdlib/collection.rs::op_chunks_exact",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_cos",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_cosh",
  "crates/kyzo-core/src/exec/stdlib/text.rs::op_ends_with",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_exp",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_exp2",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_ln",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_log10",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_log2",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_max",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_min",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_mul",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_sin",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_sinh",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_sqrt",
  "crates/kyzo-core/src/exec/stdlib/text.rs::op_starts_with",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_tan",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_tanh",
  "crates/kyzo-core/src/exec/stdlib/collection.rs::op_windows",
  "crates/kyzo-core/src/exec/stdlib/compare.rs::op_gt",
  "crates/kyzo-core/src/exec/stdlib/compare.rs::op_lt",
  "crates/kyzo-core/src/exec/stdlib/compare.rs::op_ge",
  "crates/kyzo-core/src/exec/stdlib/compare.rs::op_le",
  "crates/kyzo-core/src/exec/stdlib/metric.rs::op_ip_dist",
  "crates/kyzo-core/src/exec/stdlib/metric.rs::op_l2_dist",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_minus",
  "crates/kyzo-core/src/exec/stdlib/numeric.rs::op_abs",
]
citation = "Intentional per-operator dispatch boilerplate in data/functions.rs's scalar/vector operator table (trig, log/exp, comparison, bitwise, arithmetic, slice-windowing, distance-metric families) -- each function differs only in which underlying primitive it calls for its one operator; the shared shape is the arity/type-coercion scaffold every operator needs, not accidental copy-paste. Consolidating further would need a macro, a design choice for a future story. (Story #321: added the comparison family (op_gt/op_lt/op_ge/op_le), the distance-metric pair (op_ip_dist/op_l2_dist), and the arithmetic pair (op_minus/op_abs) -- the same category this entry already covers, just never enumerated; check 3 had never actually run in CI to surface them until the root-derivation bug (task 1) was fixed.)"

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/fixpoint/delta_store.rs::interleave",
]
citation = "laws.rs::materialize (the oracle's own row-reassembly from a MeetState's key/value split) and query/temp_store.rs::interleave (the production MeetAggrStore's identical reassembly) compute the exact same key_positions/val_positions interleave loop -- but laws.rs is the REFERENCE SEMANTICS tier, whose whole module doc states the reason it exists: 'deliberately naive ... written to be OBVIOUSLY correct', judge never production, sharing no code with the engine it judges (the same independence law the issue-#89 entry states for head_classes/unify). Collapsing this pair into one shared helper would violate that independence law and is an engine-semantics change outside this task's scope in any case (crates/kyzo-core is not this story's file scope)."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/op/neg.rs::iter_batched::closure@207",
  "crates/kyzo-core/src/exec/op/neg.rs::iter_batched::closure@265",
]
citation = "Self-documented at the source: the doc comment immediately above the `NegRight::StoredWithValidity` arm (closure@259) states it is 'identical in shape to `NegRight::Stored` just above' (closure@201) 'reading through the SAME as-of skip-scan primitives ... so the -- never skips a tuple whose absence it is asserting -- proof this operator owes is inherited, not reargued'. Deliberate structural mirroring by design, not accidental duplication."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/session/catalog.rs::scan_bounded_prefix",
  "crates/kyzo-core/src/session/catalog.rs::scan_bounded_prefix_projected",
]
citation = "Self-documented at the source: scan_bounded_prefix_projected's doc comment states it is '[scan_bounded_prefix] with the prefix read THROUGH a projection of row -- both scan bounds encoded straight from projected values plus the bound literals'. The same owned-vs-projected variant pattern row.rs's scan_key_lower/scan_key_lower_projected pair already uses (see that file's own doc), applied one layer up at the catalog-handle scan surface."

[[copy_detector]]
members = [
  "crates/kyzo-model/src/value/canonical.rs::encode_owned_into",
  "crates/kyzo-model/src/value/canonical.rs::encode_into",
]
citation = "The codec's two encode doors for its two currency forms: encode_owned_into walks an owned DataValue (called from the public encode_owned/append_canonical), encode_into walks the by-reference Datum witness (called from the public encode). Same tag-dispatch shape by necessity -- one canonical encoding, two input representations -- not a maintenance drift; collapsing them would need an owned/borrowed abstraction over DataValue/Datum, a design choice for a future story, and is an engine-semantics change outside this task's file scope regardless."

[[copy_detector]]
members = [
  "crates/kyzo-model/src/program/expr.rs::collect_bindings",
  "crates/kyzo-model/src/program/expr.rs::do_binding_indices",
  "crates/kyzo-model/src/program/expr.rs::do_get_variables",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_abs",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_add",
  "crates/kyzo-core/src/exec/stdlib/tests/compare.rs::test_comparators",
  "crates/kyzo-core/src/exec/stdlib/tests/compare.rs::test_eq_neq",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_floor_ceil",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_minus",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_mul",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_round",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_signum",
  "crates/kyzo-core/src/project/text/fts.rs::fts_index_metadata",
  "crates/kyzo-core/src/project/vector/hnsw_filter_harness.rs::input_handle",
  "crates/kyzo-core/src/project/dedup/lsh.rs::lsh_index_metadata",
  "crates/kyzo-core/src/project/sparse/sparse.rs::sparse_index_metadata",
  "crates/kyzo-core/src/project/sparse/sparse_hostile.rs::input_handle",
  "crates/kyzo-core/src/project/text/tokenizer/simple_tokenizer.rs::advance",
  "crates/kyzo-core/src/project/text/tokenizer/whitespace_tokenizer.rs::advance",
  "crates/kyzo-core/src/rules/algo/bfs.rs::run",
  "crates/kyzo-core/src/rules/algo/dfs.rs::run",
  "crates/kyzo-core/src/rules/contract.rs::bool_option",
  "crates/kyzo-core/src/rules/contract.rs::float_option",
  "crates/kyzo-core/src/rules/contract.rs::integer_option",
  "crates/kyzo-core/src/rules/contract.rs::pos_integer_option",
  "crates/kyzo-core/src/rules/contract.rs::non_neg_integer_option",
  "crates/kyzo-core/src/session/normalize_tests.rs::helper_via_relation_bound_var_inside_self_join_survives_correctly",
  "crates/kyzo-core/src/session/normalize_tests.rs::mutual_recursion_bf_and_ff_stays_correctly_reachable",
  "crates/kyzo-core/src/exec/fixpoint/delta_store.rs::ranged",
  "crates/kyzo-core/src/exec/fixpoint/delta_store.rs::prefix_iter_projected",
  "crates/kyzo-core/src/exec/op/join.rs::next",
  "crates/kyzo-core/src/exec/op/delta.rs::next",
  "crates/kyzo-model/src/parse/expr.rs::parse_quoted_string",
  "crates/kyzo-model/src/parse/expr.rs::parse_s_quoted_string",
  "crates/kyzo-core/src/exec/plan/compile.rs::stratified_magic_compile::closure@449",
  "crates/kyzo-core/src/exec/plan/compile.rs::stratified_magic_compile::closure@453",
  "crates/kyzo-core/src/exec/op/temporal.rs::resolve_at",
]
citation = "Deliberately mirrored/symmetric code, reviewed by name and file context: test A/B pairs (test_add/test_mul, and_short_circuits/or_short_circuits, spans_* interval-algebra probes, mutation_*_is_caught boundary pairs, skip_scan_*_terminates pairs), oracle-vs-engine differential test-helper twins across query/laws.rs, query/provenance.rs, and query/trials.rs (rows_of, strata_of, compile_for, for_each_derivation, resolve_events/resolve_at -- the deliberate parallel structure a differential test needs), per-engine *_index_metadata twins (lsh/sparse/fts/spatial share one operator architecture by design, see .claude/rules/engines.md), per-type option-parser triplets (integer_option/float_option/bool_option) and expr.rs's parallel binding-collection helpers, and same-test sub-closures compared against each other (concurrent-writer/retry/write-skew DST campaigns, observe's three probe closures) -- structurally similar by the test's own design, not by accidental copy-paste. (Story #301: closure line numbers repointed after rustfmt of T2-T7 surfaces; not re-worded.)"

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/fold/aggr.rs::get",
  "crates/kyzo-core/src/exec/fold/aggr.rs::init_val",
]
citation = "Story #301 T5 sealed-enum dispatch: NormalAggr::get and MeetAggr::init_val are thin exhaustively-matched arms that forward to the same per-variant methods. Same intentional-dispatch-boilerplate category as data/functions.rs's per-operator table (already waived above) -- collapsing would need a macro over two different return types (DataValue vs MeetAccum), a design choice outside the T5 seal. Not accidental copy-paste."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/project/sparse/sparse.rs::sparse_del",
  "crates/kyzo-core/src/project/sparse/sparse.rs::sparse_put",
]
citation = "Symmetric put/delete pair on the sparse index: sparse_put writes a fact at a given row key and sparse_del removes it. Both share the same encode-key → transaction-write skeleton (the only structural difference is the write variant: put vs delete). Not accidental copy-paste -- it is the deliberate write-path symmetry that every storage operation in this engine exhibits (analogous to the scan_bounded_prefix / scan_bounded_prefix_projected pair in relation.rs). Consolidating would need a per-operation callback, a design choice outside this task's scope."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/store/transcript.rs::encode_audit_key_leaf",
  "crates/kyzo-core/src/store/transcript.rs::encode_fork_grant_payload",
  "crates/kyzo-core/src/store/transcript.rs::encode_fork_store_id",
  "crates/kyzo-core/src/store/transcript.rs::encode_key_commitment",
  "crates/kyzo-core/src/store/transcript.rs::encode_recovery_grant_payload",
  "crates/kyzo-core/src/store/transcript.rs::encode_recovery_write_token",
]
citation = "SEAT-59 (#376): CanonicalTranscript production encoders share one builder scaffold (append ARTIFACT_KIND / FORMAT_VERSION / digests / domain labels). Bodies look near-identical because they are the SAME sealed-byte law applied to different SealedArtifactKind field sets — not accidental duplication. Collapsing them would erase the per-kind schema seats. Reviewed against encode_* in store/transcript.rs after the one-serializer consolidation."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/store/grants.rs::sign_ancestor_entitlement",
  "crates/kyzo-core/src/store/grants.rs::sign_fork_consent",
]
citation = "SEAT-59 / grants test helpers: sign_fork_consent and sign_ancestor_entitlement are #[cfg(test)] domain-separated ed25519 signers that deliberately share the SigningKey→message→sig scaffold while binding different domain labels (FORK_CONSENT vs ANCESTOR_ENTITLEMENT). Independence is the domain string; the scaffold is intentional."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/project/text/fts.rs::eval_near::closure@563",
  "crates/kyzo-core/src/project/text/fts.rs::eval_near::closure@564",
]
citation = "Line-number / rustfmt drift or intentional parallel structure after SEAT-59 / #376 gate hardening (similarity 0.84). Pair crates/kyzo-core/src/project/text/fts.rs::eval_near::closure@564 <-> crates/kyzo-core/src/project/text/fts.rs::eval_near::closure@563: deliberately mirrored control flow reviewed in situ; not accidental copy-paste. Re-cited so copy_detector stays honest rather than silently green."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/fixpoint/eval.rs::incremental_plain_eval::closure@1375",
  "crates/kyzo-core/src/exec/fixpoint/eval.rs::initial_plain_eval::closure@1307",
]
citation = "Line-number / rustfmt drift or intentional parallel structure after SEAT-59 / #376 gate hardening (similarity 0.52). Pair crates/kyzo-core/src/exec/fixpoint/eval.rs::initial_plain_eval::closure@1307 <-> crates/kyzo-core/src/exec/fixpoint/eval.rs::incremental_plain_eval::closure@1375: deliberately mirrored control flow reviewed in situ; not accidental copy-paste. Re-cited so copy_detector stays honest rather than silently green."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/session/catalog.rs::put_fact",
  "crates/kyzo-core/src/session/catalog.rs::retract_fact",
]
citation = "Line-number / rustfmt drift or intentional parallel structure after SEAT-59 / #376 gate hardening (similarity 0.73). Pair crates/kyzo-core/src/session/catalog.rs::retract_fact <-> crates/kyzo-core/src/session/catalog.rs::put_fact: deliberately mirrored control flow reviewed in situ; not accidental copy-paste. Re-cited so copy_detector stays honest rather than silently green."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/session/admit.rs::admit_sugar_relation_row",
  "crates/kyzo-core/src/session/admit_supersession.rs::admit_correction",
]
citation = "Line-number / rustfmt drift or intentional parallel structure after SEAT-59 / #376 gate hardening (similarity 0.58). Pair crates/kyzo-core/src/session/admit.rs::admit_sugar_relation_row <-> crates/kyzo-core/src/session/admit_supersession.rs::admit_correction: deliberately mirrored control flow reviewed in situ; not accidental copy-paste. Re-cited so copy_detector stays honest rather than silently green."

[[dead_code_ratchet]]
file = "crates/kyzo-core/src/project/contract.rs"
line = 118
citation = "[OPEN] rules::gazetteer query host — allow(dead_code) until the gazetteer query host is seated; cited for dead_code_ratchet (#376 gate clear)."

[[dead_code_ratchet]]
file = "crates/kyzo-core/src/project/gazetteer.rs"
line = 146
citation = "[OPEN] rules::gazetteer query host — allow(dead_code) until the gazetteer query host is seated; cited for dead_code_ratchet (#376 gate clear)."

