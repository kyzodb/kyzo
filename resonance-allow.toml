# Story #81: the resonance gate's one allowlist. Every entry below cites an
# issue or states a plain reason; xtask itself rejects an entry with no
# real citation (see crates/xtask/src/allowlist.rs). This file is read from the
# repo root by every one of the five checks, including inside a bite-proof's
# throwaway rsync copy (RESONANCE_ROOT points there; this file travels with
# the copy since it's part of the repo tree being copied).
#
# check 1 (derive-bypass): zero entries needed -- the tree is clean today
# (Interval and RelationId both already hand-write Deserialize; see the
# story report for the bite-proof reintroducing the historical shape).

# ---------------------------------------------------------------------------
# check 2: panic-on-hostile-bytes lint
# ---------------------------------------------------------------------------
# Decode surfaces live in kyzo-model (value plane) and store/time.rs.
# Production decode paths no longer carry length-checked `.expect(...)`
# (copy_from_slice / typed Err). Zero panic_lint waivers needed today.

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/plan/stratify.rs::aggregation_character",
]
citation = "Issue #89's ruling: the three REFERENCE-tier copies of this classification (query/laws.rs, query/provenance.rs, query/trials.rs -- they judge the engine, never each other) are consolidated into one shared query/laws.rs::head_classes, pub(crate), consumed by provenance.rs and trials.rs; likewise unify/ground/Bindings, and trials.rs::check_unify (drift, not a genuine second algorithm -- it never independently judged unify itself) folded into the same shared unify. The pairs that remain, and must remain, unwaived-but-cited: query/stratify.rs::aggregation_character is the ENGINE's copy, deliberately hand-maintained independent of the oracle's head_classes -- the refusal-corpus differential (the_oracle_refusal_corpus_is_refused, stratify.rs) is only meaningful because the engine's stratifiability boundary and the oracle's are two separately-authored implementations that happen to agree; sharing this one would collapse that differential into a tautology. Likewise query/incremental.rs's whole IVM surface (unify, collect_candidates, contribute_candidates_subset, body_bindings_from, eval_aggregating_head_incremental) is an INDEPENDENTLY-WRITTEN twin of laws.rs's incremental_eval by its own module doc: 'never a shared import ... so a bug cannot hide behind one implementation covering for the other', with the transitive proof chain (production == oracle incremental == naive recompute) resting on that independence. See the doc comment at aggregation_character's definition (stratify.rs), incremental.rs's own module doc, and head_classes'/unify's definitions (laws.rs) for the full argument. A mutant that broke the shared unify (issue #89's verification) failed laws/provenance/trials together while stratify's and incremental's own suites stayed fully green, confirming this independence is real, not assumed. (Story #321: the head_classes closure line drifted from @750 to @766 since this entry was written; repointed, not re-worded.)"

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/fixpoint/delta_store.rs::interleave",
]
citation = "laws.rs::materialize (oracle REFERENCE SEMANTICS tier: deliberately naive, judge never production) and production MeetAggrStore::interleave share a key/value reassembly loop by necessity. Independence law (issue #89): the oracle must not import production helpers or a shared bug covers for both sides; collapsing them would erase the differential proof. Sealed independence, not parked consolidation."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/session/catalog.rs::scan_bounded_prefix",
  "crates/kyzo-core/src/session/catalog.rs::scan_bounded_prefix_projected",
]
citation = "Self-documented at the source: scan_bounded_prefix_projected's doc comment states it is '[scan_bounded_prefix] with the prefix read THROUGH a projection of row -- both scan bounds encoded straight from projected values plus the bound literals'. The same owned-vs-projected variant pattern row.rs's scan_key_lower/scan_key_lower_projected pair already uses (see that file's own doc), applied one layer up at the catalog-handle scan surface."

[[copy_detector]]
members = [
  "crates/kyzo-model/src/value/canonical.rs::encode_owned_into",
  "crates/kyzo-model/src/value/canonical.rs::encode_into",
]
citation = "Seat 14a one-law codec: encode_owned_into walks owned DataValue; encode_into walks by-reference Datum. Two currency forms, one canonical byte law — same tag-dispatch shape because both emit the privately constructed encoding. Independence is the input representation (owned vs witness), not a second encode authority."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/fold/aggr.rs::get",
  "crates/kyzo-core/src/exec/fold/aggr.rs::init_val",
]
citation = "Story #301 T5 sealed-enum dispatch: NormalAggr::get and MeetAggr::init_val are thin exhaustively-matched arms that forward to the same per-variant methods. Same intentional-dispatch-boilerplate category as stdlib's per-operator table (now consolidated via higher-order helpers under WAIVER-PURITY) -- collapsing these two would need a macro over different return types (DataValue vs MeetAccum). Not accidental copy-paste."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/store/transcript.rs::encode_audit_key_leaf",
  "crates/kyzo-core/src/store/transcript.rs::encode_fork_grant_payload",
  "crates/kyzo-core/src/store/transcript.rs::encode_fork_store_id",
  "crates/kyzo-core/src/store/transcript.rs::encode_key_commitment",
  "crates/kyzo-core/src/store/transcript.rs::encode_recovery_grant_payload",
  "crates/kyzo-core/src/store/transcript.rs::encode_recovery_write_token",
]
citation = "SEAT-59 (#376): CanonicalTranscript production encoders share one builder scaffold (append ARTIFACT_KIND / FORMAT_VERSION / digests / domain labels). Bodies look near-identical because they are the SAME sealed-byte law applied to different SealedArtifactKind field sets — not accidental duplication. Collapsing them would erase the per-kind schema seats. Reviewed against encode_* in store/transcript.rs after the one-serializer consolidation."

# ---------------------------------------------------------------------------
# WAIVER-PURITY (#376): per-pair independence rulings after grab-bag split.
# Boilerplate (*_option, parse_quoted_*, tokenizer advance, stdlib scaffolds,
# admit sugar/correction, grants sign_*, put/retract, eval_near, plain-eval
# ingest) consolidated into helpers — those waivers deleted, not re-cited.
# ---------------------------------------------------------------------------

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/tests/compare.rs::test_eq_neq",
  "crates/kyzo-core/src/exec/stdlib/tests/compare.rs::test_comparators",
]
citation = "Stdlib compare suite: test_eq_neq drives ==/!=; test_comparators drives </<=/>/>=. Deliberate parallel test shape — each family is an independent adversarial corpus against its operators; sharing would collapse two refuse/accept surfaces into one tautology."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_floor_ceil",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_round",
]
citation = "Stdlib numeric suite: floor/ceil vs round are separate operator families with distinct edge tables (half-up / ties). Mirrored test harness by design, not a second implementation of either operator."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_floor_ceil",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_signum",
]
citation = "Stdlib numeric suite: floor/ceil vs signum — independent operator probes sharing only the arrange/act/assert skeleton of the numeric test module."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_round",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_signum",
]
citation = "Stdlib numeric suite: round vs signum — independent operator probes; same module test scaffold, different edge tables."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_abs",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_minus",
]
citation = "Stdlib numeric suite: abs vs unary minus — independent operators (overflow seats differ: i64::MIN abs vs neg). Parallel test shape is intentional."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_add",
  "crates/kyzo-core/src/exec/stdlib/tests/numeric.rs::test_mul",
]
citation = "Stdlib numeric suite: add vs mul — independent n-ary folds with different identities (0 vs 1) and overflow axes. Parallel test shape is intentional."

[[copy_detector]]
members = [
  "crates/kyzo-model/src/program/expr.rs::collect_bindings",
  "crates/kyzo-model/src/program/expr.rs::do_get_variables",
]
citation = "Expr walkers: collect_bindings gathers binding sites; do_get_variables gathers free variables. Same recursive descent shape because they walk one Expr sum; the visit payload (what is collected) is the independence. Not a second parser."

[[copy_detector]]
members = [
  "crates/kyzo-model/src/program/expr.rs::collect_bindings",
  "crates/kyzo-model/src/program/expr.rs::do_binding_indices",
]
citation = "Expr walkers: collect_bindings vs do_binding_indices — one collects names, the other assigns dense indices. Shared recursive-descent shape because both walk one Expr sum; independence is the visit payload (names vs dense indices). Not a second parser."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/plan/compile.rs::stratified_magic_compile::closure@450",
  "crates/kyzo-core/src/exec/plan/compile.rs::stratified_magic_compile::closure@454",
]
citation = "stratified_magic_compile: nested map closures over strata then (symbol, body) — Rules vs Fixed arms of MagicRulesOrFixed. Line-adjacent closures of one compile pass, not two compilers. Detector pairs the map layers; independence is the Rules/Fixed dispatch inside."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/stdlib/text.rs::op_regex_replace",
  "crates/kyzo-core/src/exec/stdlib/text.rs::op_regex_replace_all",
]
citation = "deliberate twin wrappers over regex crate, differ in the one call"

[[copy_detector]]
members = [
  "crates/kyzo-core/src/session/normalize_tests.rs::helper_via_relation_bound_var_inside_self_join_survives_correctly",
  "crates/kyzo-core/src/session/normalize_tests.rs::mutual_recursion_bf_and_ff_stays_correctly_reachable",
]
citation = "Normalize test helpers: self-join bound-var survival vs mutual-recursion BF/FF reachability. Deliberate parallel arrange/normalize/assert probes of two distinct normalization seats."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/fixpoint/delta_store.rs::ranged",
  "crates/kyzo-core/src/exec/fixpoint/delta_store.rs::prefix_iter_projected",
]
citation = "Temp-store iterators: ranged (key range) vs prefix_iter_projected (projected prefix). Owned-vs-projected scan variant at the delta-store surface — same category as catalog scan_bounded_prefix / scan_bounded_prefix_projected (waived above). Collapsing needs a projection parameter; kept as the two currency doors."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/rules/algo/bfs.rs::run",
  "crates/kyzo-core/src/rules/algo/dfs.rs::run",
]
citation = "Fixed-rule graph algos: BFS vs DFS are independently authored traversals (queue vs stack / different visit order). Shared FixedRule::run scaffold is the trait contract; the walk algorithms must remain separate so each can be falsified alone."

[[copy_detector]]
members = [
  "crates/kyzo-core/src/exec/op/join.rs::next",
  "crates/kyzo-core/src/exec/op/delta.rs::next",
]
citation = "RA operators: Join::next vs Delta::next — distinct operators (binary join vs delta-probe) sharing batch-advance control flow by the RA iterator contract. Independence is the match/probe law each implements; not accidental copy-paste of one join."


# ---------------------------------------------------------------------------
# check: bs_detector — seat 59 wire discriminant extract (not truncating cast)
# ---------------------------------------------------------------------------

[[bs_detector]]
pattern = "as_cast"
file = "crates/kyzo-core/src/store/replica.rs"
line = 1140
why_not_sabotage = "SEAT-59: CrossingKind is #[repr(u8)]; `self as u8` extracts the sealed discriminant — the sole wire mint. Not a truncating numeric cast of a value width."

[[bs_detector]]
pattern = "as_cast"
file = "crates/kyzo-core/src/store/transcript.rs"
line = 83
why_not_sabotage = "SEAT-59: SealedArtifactKind is #[repr(u64)]; `self as u64` extracts the sealed discriminant — the sole wire mint. Not a truncating numeric cast of a value width."

# ---------------------------------------------------------------------------
# check: bs_detector — kyzo-trials crash campaign true KEEPs (story #31)
# ---------------------------------------------------------------------------

[[bs_detector]]
pattern = "ignore_test"
file = "crates/kyzo-trials/src/crash.rs"
line = 563
why_not_sabotage = "KEEP: multi-journal segment ClearCache matrix must write past the 64 MiB journaling floor — a slow FUSE campaign. It is opt-in via --ignored with an honest reason string, not a silently skipped soft-green. The single-segment commit-boundary matrix remains in the default suite."

[[bs_detector]]
pattern = "process_exit"
file = "crates/kyzo-trials/src/crash.rs"
line = 755
why_not_sabotage = "KEEP: run_bounded hang watchdog. A stuck FUSE request parks in the kernel where SIGKILL cannot unwind the caller; process::exit(90) is the campaign verdict for the story #31 never-hang property, not a silent success path. eprintln names the invariant before exit."

[[bs_detector]]
pattern = "process_exit"
file = "crates/kyzo-trials/src/crash.rs"
line = 921
why_not_sabotage = "KEEP: crash_consistency_process_abort child deliberately aborts after staging an uncommitted write — the test subject is process-crash recovery. abort is the simulated crash, not an error swallow; the parent asserts the child did not exit cleanly and checks reopen contents."

[[bs_detector]]
pattern = "ignore_test"
file = "crates/kyzo-trials/src/gauntlet.rs"
line = 1448
why_not_sabotage = "KEEP: disclosed open seat `demand-rewriter-generative-gap`. Cap1's RuleBody seam is post-stratification and cannot exercise magic-set demand rewriting; the ignored test names the missing public door (`stratified_magic_compile` / Engine generative magic-vs-bypass) and fails loud if un-ignored before that door exists. Not a soft-green skip — the reason string is the standing obligation."

# ---------------------------------------------------------------------------
# check: bs_detector — kyzo-crashfs true KEEPs (seed/mix + FUSE API doors)
# ---------------------------------------------------------------------------

[[bs_detector]]
pattern = "wrapping_arith"
file = "crates/kyzo-crashfs/src/identity.rs"
line = 20
why_not_sabotage = "KEEP: wrap_mul is the sole SeedMix door for FNV-1a / splitmix identity hashing. Wrap is the published mix contract (not overflow loss); adjacent INVARIANT(SeedMix) names it. Every fault/sim identity and ppm draw routes through this one line."

[[bs_detector]]
pattern = "wrapping_arith"
file = "crates/kyzo-crashfs/src/identity.rs"
line = 27
why_not_sabotage = "KEEP: wrap_add is the sole SeedMix step-add door for splitmix64 PRNG advance. Wrap is the published mix contract; adjacent INVARIANT(SeedMix) names it."

[[bs_detector]]
pattern = "ok_drop"
file = "crates/kyzo-crashfs/src/passthrough.rs"
line = 190
why_not_sabotage = "KEEP: fuse_ok! expands to fuser Reply*::ok — the FUSE kernel-success reply door, not Result::ok error-drop. One macro, every success path."

[[bs_detector]]
pattern = "default_impl"
file = "crates/kyzo-crashfs/src/sim.rs"
line = 179
why_not_sabotage = "KEEP: FaultConfig::default is FaultConfig::none — all ppm rates zero, pure MVCC/SSI. Trials DST uses Default as the no-fault pole of the ppm dial; not a domain sentinel inventing meaning."

[[bs_detector]]
file = "crates/kyzo-lsp/src/translate.rs"
line = 59
pattern = "expect"
why_not_sabotage = "KEEP: LspUtf16Column — UTF-16 column count of a single line fits u32 by LSP position law. TryFrom.expect is the typed door; inventing 0 would lie to the editor."

[[bs_detector]]
file = "crates/kyzo-lsp/src/translate.rs"
line = 60
pattern = "expect"
why_not_sabotage = "KEEP: LspLine — line index from LineIndex fits u32. TryFrom.expect is the typed door."

[[bs_detector]]
file = "crates/kyzo-lsp/src/translate.rs"
line = 72
pattern = "expect"
why_not_sabotage = "KEEP: LspLine — line index fits u32/usize. TryFrom.expect is the typed door."

[[bs_detector]]
file = "crates/kyzo-lsp/src/translate.rs"
line = 53
pattern = "expect"
why_not_sabotage = "KEEP: LineIndexNonEmpty — line_starts always begins at byte 0; checked_sub on binary_search Err is the sole line pick. Loud INVARIANT expect is fail-closed, not silent clamp theater."

[[bs_detector]]
file = "crates/kyzo-lsp/src/translate.rs"
line = 89
pattern = "expect"
why_not_sabotage = "KEEP: LspUtf16Width — char::len_utf16 is 1 or 2; fits u32. TryFrom.expect is the typed door for the width add."

[[bs_detector]]
file = "crates/kyzo-core/src/session/observe.rs"
line = 422
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(deep_verify_mismatch_count) — Vec::len fits u64 on all supported targets. TryFrom.expect is the typed door; u64::MAX would invent a mismatch count."

[[bs_detector]]
file = "crates/kyzo-core/src/session/observe.rs"
line = 682
pattern = "expect"
why_not_sabotage = "KEEP: EventCallbackRegistry RwLock poison is fail-closed — refuse silent continue. Helper is the sole observe-registry lock door; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/observe.rs"
line = 689
pattern = "expect"
why_not_sabotage = "KEEP: EventCallbackRegistry RwLock poison is fail-closed — refuse silent continue. Helper is the sole observe-registry lock door; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-oracle/src/lib.rs"
line = 209
pattern = "as_cast"
why_not_sabotage = "KEEP: RunningTotal::Approx pole — i128/i64→f64 is the published sum overflow/float promotion, not a truncating width costume."

[[bs_detector]]
file = "crates/kyzo-oracle/src/lib.rs"
line = 213
pattern = "as_cast"
why_not_sabotage = "KEEP: RunningTotal::Approx pole — i128/i64→f64 is the published sum overflow/float promotion, not a truncating width costume."

[[bs_detector]]
file = "crates/kyzo-oracle/src/lib.rs"
line = 216
pattern = "as_cast"
why_not_sabotage = "KEEP: RunningTotal::Approx pole — i128/i64→f64 is the published sum overflow/float promotion, not a truncating width costume."

[[bs_detector]]
file = "crates/kyzo-oracle/src/lib.rs"
line = 226
pattern = "as_cast"
why_not_sabotage = "KEEP: RunningTotal::Approx pole — i128/i64→f64 is the published sum overflow/float promotion, not a truncating width costume."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1038
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not silent clamp."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1049
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not silent clamp."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1378
pattern = "expect"
why_not_sabotage = "KEEP: oracle law corpus test premise — expect fails loud if a known-good program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1409
pattern = "expect"
why_not_sabotage = "KEEP: oracle law corpus test premise — expect fails loud if a known-good program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1438
pattern = "expect"
why_not_sabotage = "KEEP: oracle law corpus test premise — expect fails loud if a known-good program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1458
pattern = "expect"
why_not_sabotage = "KEEP: test literal NonZeroU32 — expect names the nonzero premise; inventing Option would soft-green the budget door."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1474
pattern = "expect"
why_not_sabotage = "KEEP: test literal NonZeroU32 — expect names the nonzero premise; inventing Option would soft-green the budget door."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1476
pattern = "expect"
why_not_sabotage = "KEEP: budget/oracle corpus test premise — expect fails the test loud if the oracle refuses a known-good program."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1477
pattern = "expect"
why_not_sabotage = "KEEP: budget/oracle corpus test premise — expect fails the test loud if the oracle refuses a known-good program."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1501
pattern = "expect"
why_not_sabotage = "KEEP: oracle law corpus test premise — expect fails loud if a known-good program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1540
pattern = "expect"
why_not_sabotage = "KEEP: oracle law corpus test premise — expect fails loud if a known-good program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/eval.rs"
line = 1563
pattern = "expect"
why_not_sabotage = "KEEP: oracle law corpus test premise — expect fails loud if a known-good program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/incremental.rs"
line = 685
pattern = "expect"
why_not_sabotage = "KEEP: incremental corpus test premise — expect fails loud if a known-good non-recursive program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/incremental.rs"
line = 701
pattern = "expect"
why_not_sabotage = "KEEP: incremental corpus test premise — expect fails loud if a known-good non-recursive program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/incremental.rs"
line = 702
pattern = "expect"
why_not_sabotage = "KEEP: incremental corpus test premise — expect fails loud if a known-good non-recursive program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/incremental.rs"
line = 793
pattern = "expect"
why_not_sabotage = "KEEP: incremental corpus test premise — expect fails loud if a known-good non-recursive program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/incremental.rs"
line = 831
pattern = "expect"
why_not_sabotage = "KEEP: incremental corpus test premise — expect fails loud if a known-good non-recursive program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/incremental.rs"
line = 889
pattern = "expect"
why_not_sabotage = "KEEP: incremental corpus test premise — expect fails loud if a known-good non-recursive program refuses."

[[bs_detector]]
file = "crates/kyzo-oracle/src/temporal.rs"
line = 350
pattern = "expect"
why_not_sabotage = "KEEP: temporal fixture premise — Event/compose of hand-built valid instants; expect fails loud if the door refuses a known-valid instant."

[[bs_detector]]
file = "crates/kyzo-oracle/src/temporal.rs"
line = 353
pattern = "expect"
why_not_sabotage = "KEEP: temporal fixture premise — Event/compose of hand-built valid instants; expect fails loud if the door refuses a known-valid instant."

[[bs_detector]]
file = "crates/kyzo-oracle/src/temporal.rs"
line = 356
pattern = "expect"
why_not_sabotage = "KEEP: temporal fixture premise — Event/compose of hand-built valid instants; expect fails loud if the door refuses a known-valid instant."

[[bs_detector]]
file = "crates/kyzo-oracle/src/temporal.rs"
line = 587
pattern = "expect"
why_not_sabotage = "KEEP: temporal fixture premise — Event/compose of hand-built valid instants; expect fails loud if the door refuses a known-valid instant."

[[bs_detector]]
file = "crates/kyzo-model/src/envelope/arrow.rs"
line = 855
pattern = "expect"
why_not_sabotage = "KEEP: lawful Arrow width TryFrom.expect — fixture/plan premise, not silent clamp."

[[bs_detector]]
file = "crates/kyzo-model/src/envelope/arrow.rs"
line = 870
pattern = "expect"
why_not_sabotage = "KEEP: lawful Arrow width TryFrom.expect — fixture/plan premise, not silent clamp."

[[bs_detector]]
file = "crates/kyzo-model/src/envelope/json.rs"
line = 130
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(serde_json_*) — serde_json finite/unique doors; expect is fail-closed not Null costume."

[[bs_detector]]
file = "crates/kyzo-model/src/envelope/json.rs"
line = 135
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(serde_json_*) — serde_json finite/unique doors; expect is fail-closed not Null costume."

[[bs_detector]]
file = "crates/kyzo-model/src/envelope/json.rs"
line = 149
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(serde_json_*) — serde_json finite/unique doors; expect is fail-closed not Null costume."

[[bs_detector]]
file = "crates/kyzo-model/src/value/bytes_qty.rs"
line = 59
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/bytes_qty.rs"
line = 103
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/bytes_qty.rs"
line = 138
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/cell.rs"
line = 174
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/cell.rs"
line = 208
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/exec.rs"
line = 202
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(code_fits_usize) — domain code TryFrom.expect is the typed door."

[[bs_detector]]
file = "crates/kyzo-model/src/value/kind/vector.rs"
line = 104
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/prefix.rs"
line = 64
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/prefix.rs"
line = 84
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(len_fits_u32) — slice len TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/prefix.rs"
line = 86
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(len_fits_u32) — slice len TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 274
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 329
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(needle_len_fits_u32) — needle len TryFrom.expect is the typed door."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 393
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_max_fits_usize) — u32::MAX→usize TryFrom.expect is the typed door on supported widths."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 777
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 781
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 849
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 857
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 861
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 872
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 875
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 962
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_max_fits_usize) — u32::MAX→usize TryFrom.expect is the typed door on supported widths."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 979
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1046
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1093
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1660
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_max_fits_usize) — u32::MAX→usize TryFrom.expect is the typed door on supported widths."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1663
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_max_fits_usize) — u32::MAX→usize TryFrom.expect is the typed door on supported widths."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1687
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1695
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1745
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1756
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1812
pattern = "expect"
why_not_sabotage = "KEEP: arena adversary fixture — ChunkId/ByteLen of small literals; expect fails loud if from_usize refuses."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1814
pattern = "expect"
why_not_sabotage = "KEEP: arena adversary fixture — ChunkId/ByteLen of small literals; expect fails loud if from_usize refuses."

[[bs_detector]]
file = "crates/kyzo-model/src/value/arena.rs"
line = 1909
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(u32_fits_usize) — u32→usize TryFrom.expect is the typed door; inventing MAX would be err_to_max theater."

[[bs_detector]]
file = "crates/kyzo-core/src/project/residency.rs"
line = 62
pattern = "expect"
why_not_sabotage = "KEEP: residency mutex poison is fail-closed — helper is the sole lock door; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/project/residency.rs"
line = 68
pattern = "expect"
why_not_sabotage = "KEEP: residency mutex poison is fail-closed — helper is the sole lock door; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/project/residency.rs"
line = 218
pattern = "expect"
why_not_sabotage = "KEEP: test RelationId::new(1) premise — expect fails loud if below-cap door refuses a literal 1."

[[bs_detector]]
file = "crates/kyzo-core/src/project/residency.rs"
line = 221
pattern = "expect"
why_not_sabotage = "KEEP: poison-adversary setup — expect on a fresh mutex before deliberate panic_any; not a production swallow."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fixpoint/eval.rs"
line = 394
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(millis_fit_u64) — Duration::as_millis→u64 TryFrom.expect for deadline reporting."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fixpoint/eval.rs"
line = 573
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(usize_fits_u64) — in_flight count TryFrom.expect."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fixpoint/eval.rs"
line = 1253
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(admitted_fits_u64) — merge admission count TryFrom.expect."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fixpoint/eval.rs"
line = 1571
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(effective_fits_usize) — meet admission count TryFrom.expect."

[[bs_detector]]
file = "crates/kyzo-core/src/session/footprint.rs"
line = 404
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(fence_pressure_fits_u64) — live fenced count TryFrom.expect."

[[bs_detector]]
file = "crates/kyzo-core/src/project/text/tokenizer/tokenized_string.rs"
line = 99
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT(token_len_fits_i64) — token count TryFrom.expect."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fold/sketch/count_min.rs"
line = 182
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fold/sketch/hll.rs"
line = 202
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fold/sketch/hll.rs"
line = 209
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fold/sketch/hll.rs"
line = 211
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fold/sketch/mod.rs"
line = 145
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/exec/fold/sketch/mod.rs"
line = 227
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/project/current.rs"
line = 100
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/project/current.rs"
line = 415
pattern = "expect"
why_not_sabotage = "KEEP: test RelationId::new premise — expect fails loud if below-cap door refuses."

[[bs_detector]]
file = "crates/kyzo-core/src/project/current.rs"
line = 418
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/project/dedup/lsh.rs"
line = 552
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/project/spatial/index.rs"
line = 300
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/project/spatial/index.rs"
line = 574
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/project/spatial/index.rs"
line = 575
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/project/text/mod.rs"
line = 545
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/project/text/mod.rs"
line = 550
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/project/vector/hnsw.rs"
line = 1814
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/session/admit.rs"
line = 410
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/admit.rs"
line = 418
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/admit.rs"
line = 427
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/admit.rs"
line = 436
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/admit.rs"
line = 462
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/admit.rs"
line = 3354
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/session/admit.rs"
line = 3360
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/session/db.rs"
line = 406
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/db.rs"
line = 417
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/db.rs"
line = 431
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/session/db.rs"
line = 447
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/store/merkle.rs"
line = 139
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/store/merkle.rs"
line = 1255
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/store/sweep.rs"
line = 132
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/store/sweep.rs"
line = 240
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/store/sweep.rs"
line = 247
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/store/sweep.rs"
line = 307
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/store/sweep.rs"
line = 317
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/store/sweep.rs"
line = 336
pattern = "expect"
why_not_sabotage = "KEEP: mutex/rwlock poison is fail-closed — refuse silent continue; into_inner would hide poison."

[[bs_detector]]
file = "crates/kyzo-core/src/store/transcript.rs"
line = 1705
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/store/verify_walk.rs"
line = 448
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."

[[bs_detector]]
file = "crates/kyzo-core/src/store/verify_walk.rs"
line = 457
pattern = "expect"
why_not_sabotage = "KEEP: INVARIANT TryFrom.expect — fail-closed width door, not Err→MAX clamp theater."


# ---------------------------------------------------------------------------
# check: bs_detector — deliberate poison-adversary panic_bang (fail-closed mutex)
# ---------------------------------------------------------------------------

[[bs_detector]]
file = "crates/kyzo-core/src/project/current.rs"
line = 420
pattern = "panic_bang"
why_not_sabotage = "KEEP: poison-adversary setup — deliberate panic while holding the segments mutex is the test subject; catch_unwind asserts fail-closed refuse. Not a production swallow."

[[bs_detector]]
file = "crates/kyzo-core/src/project/residency.rs"
line = 224
pattern = "panic_bang"
why_not_sabotage = "KEEP: poison-adversary setup — deliberate panic while holding the marks mutex is the test subject; catch_unwind asserts fail-closed refuse. Not a production swallow."
