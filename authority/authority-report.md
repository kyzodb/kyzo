# Type Authority Graph — drift report

Generated by `scripts/authority-graph` (v1) over `crates/kyzo-core/src` (175 files). Regenerate with `cargo xtask authority --write`; the gate fails if this file is stale (`cargo xtask authority`).

## Layer spine

| layer | holds |
|---|---|
| `value` | Tuple · Domain · ExecRows/ExecDedup · EncodedKey |
| `runtime-catalog` | CatalogGeneration · RelationGeneration · IndexGeneration |
| `storage` | encoded-key / order integrity (consumes EncodedKey) |
| `engines` | ResidentIndexKey (rebuildable projections) |
| `query` | QueryDomainAdmission (admission into execution) |
| `record` | RecordId / KyzoRecord identity |

## Declared authorities

| authority | layer | status | declared at | owns |
|---|---|---|---|---|
| `Domain` | `value` | established #119 | `crates/kyzo-core/src/data/value/column.rs:55` | arena+epoch+visibility admission; a raw u32 code is meaningful only under a proven Domain; cross-Domain code comparison is invalid |
| `EncodedKey` | `value` | established #119 | `crates/kyzo-core/src/data/value/row.rs:247` | canonical storage identity in the memcmp keyspace; bytewise order equals DataValue structural order (byte-order law) |
| `ExecDedup` | `value` | established #119 | `crates/kyzo-core/src/data/value/exec.rs:189` | the fixpoint dedup identity: packed admitted code tuples, u32-slice equality, no canonical encode in the hot loop |
| `ExecRows` | `value` | established #119 | `crates/kyzo-core/src/data/value/exec.rs:53` | the admitted execution currency; codes are unforgeable (private field, no raw-code door) |
| `Tuple` | `value` | established #300 | `crates/kyzo-core/src/data/value/mod.rs:533` | row identity as an ordered `DataValue` sequence, distinct from any bare `Vec<DataValue>` |

## Legal conversions (edges)

- `Domain` → `ExecRows` — Rows::admit(observer) -> AdmittedRows, under this Domain
- `ExecRows` → `EncodedKey` — canonical bytes only at a storage/output boundary, never durable identity
- `ExecDedup` → `ExecRows` — to_exec: the distinct rows, same Domain
- `EncodedKey` → `Domain` — admitted into an arena via push_encoded

## Planned (conditional) authorities

- `RecordId` (`record`) — created by #128 only if it proceeds

## Findings (18 live, 3 allowlisted)

| class | count |
|---|---|
| `blob-meaning` | 9 |
| `freshness-twin` | 1 |
| `missing-authority` | 5 |
| `searchra-decoded-tuples` | 3 |

### blob-meaning

- `crates/kyzo-core/src/engines/hnsw.rs:491` — generic byte blob carrying meaning outside the byte plane
  `vec_hash: Vec<u8>,`
- `crates/kyzo-core/src/engines/hnsw.rs:530` — generic byte blob carrying meaning outside the byte plane
  `entry_key: Vec<u8>,`
- `crates/kyzo-core/src/engines/hnsw.rs:2072` — generic byte blob carrying meaning outside the byte plane
  `key: Vec<u8>,`
- `crates/kyzo-core/src/engines/lsh.rs:158` — generic byte blob carrying meaning outside the byte plane
  `pub(crate) perms: Vec<u8>,`
- `crates/kyzo-core/src/query/levels.rs:63` — generic byte blob carrying meaning outside the byte plane
  `values: Vec<u8>,`
- `crates/kyzo-core/src/query/levels.rs:556` — generic byte blob carrying meaning outside the byte plane
  `lower: Vec<u8>,`
- `crates/kyzo-core/src/query/levels.rs:557` — generic byte blob carrying meaning outside the byte plane
  `upper: Vec<u8>,`
- `crates/kyzo-core/src/query/levels.rs:753` — generic byte blob carrying meaning outside the byte plane
  `lower: Vec<u8>,`
- `crates/kyzo-core/src/query/levels.rs:754` — generic byte blob carrying meaning outside the byte plane
  `upper: Vec<u8>,`
### freshness-twin

- `crates/kyzo-core/src/engines/segments.rs:71` — per-projection Watermark freshness twin of the catalog generation authority (#135 / #301 T7)
  `pub(crate) struct Watermark(u64);`
### missing-authority

- `crates/kyzo-core/src` — expected authority (layer runtime-catalog) does not exist yet — owed by #135
- `crates/kyzo-core/src` — expected authority (layer runtime-catalog) does not exist yet — owed by #135
- `crates/kyzo-core/src` — expected authority (layer query) does not exist yet — owed by #122
- `crates/kyzo-core/src` — expected authority (layer runtime-catalog) does not exist yet — owed by #135
- `crates/kyzo-core/src` — expected authority (layer engines) does not exist yet — owed by #122
### searchra-decoded-tuples

- `crates/kyzo-core/src/query/ra/search.rs:107` — engine hits flow as decoded tuples, not admitted codes (QueryDomainAdmission owed by #122)
  `let search = move |row: &[DataValue]| -> Result<Vec<Tuple>> {`
- `crates/kyzo-core/src/query/ra/search.rs:176` — engine hits flow as decoded tuples, not admitted codes (QueryDomainAdmission owed by #122)
  `hits: Vec<Tuple>,`
- `crates/kyzo-core/src/query/ra/search.rs:178` — engine hits flow as decoded tuples, not admitted codes (QueryDomainAdmission owed by #122)
  `search: Box<dyn FnMut(&[DataValue]) -> Result<Vec<Tuple>> + 'a>,`

## Allowlisted (intentional boundaries)

- `crates/kyzo-core/src/data/value/row.rs:434` `illegal-escape` — read-only &[u8] view: the one legal EncodedKey -> stored-bytes conversion; field private, no DerefMut, every mint validates — bytes can be read, never forged
- `crates/kyzo-core/src/data/value/arena.rs:2372` `unchecked-constructor` — named corruption/bypass TEST asserting that a forged stamp panics — it proves the door is shut, it is not a door
- `crates/kyzo-core/src/data/value/wide/validity.rs:43` `unchecked-constructor` — total boundary constructor: every i64 is a valid validity-timestamp micros value; parse-at-boundary, not an authority bypass

