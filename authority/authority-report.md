# Type Authority Graph — drift report

Generated by `scripts/authority-graph` (v1) over `crates/kyzo-core/src` (179 files). Regenerate with `cargo xtask authority --write`; the gate fails if this file is stale (`cargo xtask authority`).

## Layer spine

| layer | holds |
|---|---|
| `value` | Tuple · Domain · ExecRows/ExecDedup · TupleKey/StorageKey |
| `runtime-catalog` | CatalogGeneration · RelationGeneration · IndexGeneration |
| `storage` | encoded-key / order integrity (consumes StorageKey) |
| `engines` | ResidentIndexKey (rebuildable projections) |
| `query` | QueryDomainAdmission (admission into execution) |
| `record` | RecordId / KyzoRecord identity |

## Declared authorities

| authority | layer | status | declared at | owns |
|---|---|---|---|---|
| `CatalogGeneration` | `runtime-catalog` | established #337 | `crates/kyzo-core/src/runtime/generation.rs:22` | the one catalog-meaning freshness clock; sole door that mints projection Generation stamps from a proven counter |
| `Domain` | `value` | established #119 | `crates/kyzo-core/src/data/value/column.rs:68` | arena+epoch+visibility admission; a raw u32 code is meaningful only under a proven Domain; cross-Domain code comparison is invalid |
| `ExecDedup` | `value` | established #119 | `crates/kyzo-core/src/data/value/exec.rs:220` | the fixpoint dedup identity: packed admitted code tuples, u32-slice equality, no canonical encode in the hot loop |
| `ExecRows` | `value` | established #119 | `crates/kyzo-core/src/data/value/exec.rs:57` | the admitted execution currency; codes are unforgeable (private field, no raw-code door) |
| `IndexGeneration` | `runtime-catalog` | established #337 | `crates/kyzo-core/src/runtime/generation.rs:77` | per-index monotone rebuild counter that witnesses catalog freshness for one index |
| `QueryDomainAdmission` | `query` | established #337 | `crates/kyzo-core/src/query/ra/search.rs:39` | admission of engine search hits into the query execution currency as codes; decoded engine tuples never cross the SearchRA seam |
| `RelationGeneration` | `runtime-catalog` | established #337 | `crates/kyzo-core/src/runtime/generation.rs:52` | per-relation monotone write counter that witnesses catalog freshness for one relation |
| `ResidentIndexKey` | `engines` | established #337 | `crates/kyzo-core/src/engines/projection.rs:105` | rebuildable projection residency identity — a sealed kind's cache key under one Generation |
| `StorageKey` | `value` | established #303 | `crates/kyzo-core/src/data/value/row.rs:300` | canonical storage identity in the memcmp keyspace; bytewise order equals DataValue structural order (byte-order law) |
| `Tuple` | `value` | established #300 | `crates/kyzo-core/src/data/value/mod.rs:514` | row identity as an ordered `DataValue` sequence, distinct from any bare `Vec<DataValue>` |
| `TupleKey` | `value` | established #303 | `crates/kyzo-core/src/data/value/row.rs:282` | bare-tuple memcmp identity (no relation prefix); bytewise order equals DataValue structural order (byte-order law) |

## Legal conversions (edges)

- `Domain` → `ExecRows` — Rows::admit(observer) -> AdmittedRows, under this Domain
- `ExecRows` → `StorageKey` — canonical bytes only at a storage/output boundary, never durable identity
- `ExecDedup` → `ExecRows` — to_exec: the distinct rows, same Domain
- `TupleKey` → `StorageKey` — prefix with RelationId at the storage boundary
- `StorageKey` → `Domain` — admitted into an arena via push_encoded
- `CatalogGeneration` → `Generation` — projection seal/classify stamp
- `RelationGeneration` → `CatalogGeneration` — lift into the catalog clock
- `IndexGeneration` → `CatalogGeneration` — lift into the catalog clock

## Planned (conditional) authorities

- `RecordId` (`record`) — created by #128 only if it proceeds

## Findings (2 live, 4 allowlisted)

| class | count |
|---|---|
| `error-downcast` | 2 |

### error-downcast

- `crates/kyzo-core/src/storage/retry.rs:27` — Report.downcast_ref recovering typed errors in product code
  `.downcast_ref::<ConflictError>`
- `crates/kyzo-core/src/storage/retry.rs:29` — Report.downcast_ref recovering typed errors in product code
  `.downcast_ref::<CommitFailure>`

## Allowlisted (intentional boundaries)

- `crates/kyzo-core/src/data/value/row.rs:417` `illegal-escape` — read-only &[u8] view: the one legal TupleKey -> stored-bytes conversion; field private, no DerefMut, every mint validates — bytes can be read, never forged
- `crates/kyzo-core/src/data/value/row.rs:430` `illegal-escape` — read-only &[u8] view: the one legal StorageKey -> stored-bytes conversion; field private, no DerefMut, every mint validates — bytes can be read, never forged
- `crates/kyzo-core/src/data/value/arena.rs:2818` `unchecked-constructor` — named corruption/bypass TEST asserting that a forged stamp refuses typed — it proves the door is shut, it is not a door
- `crates/kyzo-core/src/data/value/wide/validity.rs:60` `unchecked-constructor` — total boundary constructor: every i64 is a valid validity-timestamp micros value; parse-at-boundary, not an authority bypass

