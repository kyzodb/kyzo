# Type Authority Graph — drift report

Generated by `scripts/authority-graph` (v1) over `crates/kyzo-core/src` (206 files). Regenerate with `cargo xtask authority --write`; the gate fails if this file is stale (`cargo xtask authority`).

## Layer spine

| layer | holds |
|---|---|
| `value` | Tuple · Domain · ExecRows/ExecDedup · TupleKey/StorageKey |
| `runtime-catalog` | CatalogGeneration · RelationGeneration · IndexGeneration |
| `storage` | encoded-key / order integrity (consumes StorageKey) |
| `engines` | ResidentIndexKey (rebuildable projections) |
| `query` | QueryDomainAdmission (admission into execution) |
| `record` | RecordId / KyzoRecord identity |

## Declared authorities

| authority | layer | status | declared at | owns |
|---|---|---|---|---|
| `CatalogGeneration` | `runtime-catalog` | established #337 | `crates/kyzo-core/src/session/generation.rs:22` | the one catalog-meaning freshness clock; sole door that mints projection Generation stamps from a proven counter |
| `IndexGeneration` | `runtime-catalog` | established #337 | `crates/kyzo-core/src/session/generation.rs:77` | per-index monotone rebuild counter that witnesses catalog freshness for one index |
| `RelationGeneration` | `runtime-catalog` | established #337 | `crates/kyzo-core/src/session/generation.rs:52` | per-relation monotone write counter that witnesses catalog freshness for one relation |
| `ResidentIndexKey` | `engines` | established #337 | `crates/kyzo-core/src/project/projection.rs:119` | rebuildable projection residency identity — a sealed kind's cache key under one Generation |

## Legal conversions (edges)

- `CatalogGeneration` → `Generation` — projection seal/classify stamp
- `RelationGeneration` → `CatalogGeneration` — lift into the catalog clock
- `IndexGeneration` → `CatalogGeneration` — lift into the catalog clock

## Planned (conditional) authorities

- `RecordId` (`record`) — created by #128 only if it proceeds

## Findings (34 live, 0 allowlisted)

| class | count |
|---|---|
| `duplicate-generation-counter` | 6 |
| `error-downcast` | 4 |
| `layer-mismatch` | 4 |
| `missing-authority` | 7 |
| `stale-allowlist` | 4 |
| `unchecked-constructor` | 9 |

### duplicate-generation-counter

- `crates/kyzo-core/src/session/footprint.rs:197` — raw-integer generation counter (catalog generations are the one validity authority, #135)
  `catalog_generation: u64,`
- `crates/kyzo-core/src/session/generation.rs:31` — CatalogGeneration declared outside the runtime catalog seam
  `pub(crate) struct CatalogGeneration(u64);`
- `crates/kyzo-core/src/session/generation.rs:61` — RelationGeneration declared outside the runtime catalog seam
  `pub(crate) struct RelationGeneration(u64);`
- `crates/kyzo-core/src/session/generation.rs:86` — IndexGeneration declared outside the runtime catalog seam
  `pub(crate) struct IndexGeneration(u64);`
- `crates/kyzo-core/src/store/commit_cap.rs:110` — raw-integer generation counter (catalog generations are the one validity authority, #135)
  `generation: u64,`
- `crates/kyzo-core/src/store/replica.rs:321` — raw-integer generation counter (catalog generations are the one validity authority, #135)
  `catalog_generation: u64,`
### error-downcast

- `crates/kyzo-core/src/project/contract.rs:262` — Report.downcast_ref recovering typed errors in product code
  `.downcast_ref::<DecodeError>`
- `crates/kyzo-core/src/session/verify.rs:400` — Report.downcast_ref recovering typed errors in product code
  `.downcast_ref::<ProvenanceLimitExceeded>`
- `crates/kyzo-core/src/session/verify.rs:405` — Report.downcast_ref recovering typed errors in product code
  `.downcast_ref::<ProvenanceUnsupported>`
- `crates/kyzo-core/src/session/verify.rs:424` — Report.downcast_ref recovering typed errors in product code
  `.downcast_ref::<ProvenanceLimitExceeded>`
### layer-mismatch

- `crates/kyzo-core/src/project/projection.rs:119` — declared layer 'engines' does not own path crates/kyzo-core/src/project/projection.rs
  `@authority ResidentIndexKey`
- `crates/kyzo-core/src/session/generation.rs:22` — declared layer 'runtime-catalog' does not own path crates/kyzo-core/src/session/generation.rs
  `@authority CatalogGeneration`
- `crates/kyzo-core/src/session/generation.rs:52` — declared layer 'runtime-catalog' does not own path crates/kyzo-core/src/session/generation.rs
  `@authority RelationGeneration`
- `crates/kyzo-core/src/session/generation.rs:77` — declared layer 'runtime-catalog' does not own path crates/kyzo-core/src/session/generation.rs
  `@authority IndexGeneration`
### missing-authority

- `crates/kyzo-core/src` — expected authority (layer value) does not exist yet — owed by #119
- `crates/kyzo-core/src` — expected authority (layer value) does not exist yet — owed by #119
- `crates/kyzo-core/src` — expected authority (layer value) does not exist yet — owed by #119
- `crates/kyzo-core/src` — expected authority (layer query) does not exist yet — owed by #122
- `crates/kyzo-core/src` — expected authority (layer value) does not exist yet — owed by #303
- `crates/kyzo-core/src` — expected authority (layer value) does not exist yet — owed by #126
- `crates/kyzo-core/src` — expected authority (layer value) does not exist yet — owed by #303
### stale-allowlist

- `crates/kyzo-core/src/data/value/arena.rs` — allowlist entry matches nothing — delete it
- `crates/kyzo-core/src/data/value/row.rs` — allowlist entry matches nothing — delete it
- `crates/kyzo-core/src/data/value/row.rs` — allowlist entry matches nothing — delete it
- `crates/kyzo-core/src/data/value/wide/validity.rs` — allowlist entry matches nothing — delete it
### unchecked-constructor

- `crates/kyzo-core/src/project/residency.rs:114` — raw-door constructor 'forget'
  `pub(crate) fn forget(&self, relation: RelationId) {`
- `crates/kyzo-core/src/store/authority.rs:31` — raw-door constructor 'from_raw'
  `pub(crate) fn from_raw(raw: u64) -> Self {`
- `crates/kyzo-core/src/store/crypto.rs:43` — raw-door constructor 'from_raw'
  `pub fn from_raw(raw: u64) -> Self {`
- `crates/kyzo-core/src/store/epoch.rs:33` — raw-door constructor 'from_raw'
  `pub(crate) fn from_raw(raw: u64) -> Self {`
- `crates/kyzo-core/src/store/failure.rs:73` — raw-door constructor 'from_raw'
  `pub const fn from_raw(raw: u64) -> Self {`
- `crates/kyzo-core/src/store/nonce.rs:57` — raw-door constructor 'from_raw'
  `pub(crate) fn from_raw(raw: u64) -> Self {`
- `crates/kyzo-core/src/store/sweep.rs:51` — raw-door constructor 'from_raw'
  `pub(crate) fn from_raw(raw: u64) -> Self {`
- `crates/kyzo-core/src/store/sweep.rs:80` — raw-door constructor 'from_raw'
  `pub(crate) fn from_raw(raw: u64) -> Self {`
- `crates/kyzo-core/src/store/transcript.rs:90` — raw-door constructor 'from_raw'
  `pub const fn from_raw(raw: u16) -> Self {`

