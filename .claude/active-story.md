story: none — between stories
branch: main
status: no active story

note:
  - #119 (the value plane) is sealed and merged to main (merge commit on main's
    first-parent line). No story is in flight.
  - Do NOT start a story on your own. Await the operator's explicit "start #NNN".
  - Next up when the operator says go: #126 (Tuple newtype hardening) — a measured
    565-error / ~40-file compiler-guided refactor, on its own fresh branch.
