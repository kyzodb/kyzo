<!-- Copyright 2026, The KyzoDB Authors. MPL-2.0. -->
<!-- KIND: proof (`benchmark` / `trial` / `demo` / `public-proof`). -->
<!-- Substance floor enforced; the shape below is the canon reviewers hold you to. -->

One-sentence claim this proof establishes, and who can independently rerun it.

## Claim
The single measurable/observable assertion. For a benchmark: the metric and the competitor.

## Oracle / baseline
What decides pass/fail: the reference implementation, the recorded baseline commit, the dataset.
A perf claim closes on the bench lane's re-measurement of the merged change, never on our own numbers.

## Dataset / fixtures
The exact inputs, their provenance, and how they are fetched/generated deterministically.

## Tolerance / bar
The kill threshold or acceptance bar, with the reason it is set there (not an invented round number).

## Rerun
The exact command anyone runs to reproduce, in the container.
